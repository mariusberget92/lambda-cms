Block-editor:
	- [done] When adding a background image via Advanced settings in the block editor we should be able to bind it dynamically to forexample the featured image of a post.
	- [done] Border-radius the same controlls used for padding and spacing so we can controll radius each side.
	- [done] Border-radius needs to be a setting for a container aswell.
	- [done] Image block clicking library/url does not show the media picker. Also, should be able to bind dynamically.
	- [done] Clicking update/save when in the blockeditor should not redirect the user anywhere, the user should stay in the block editor.
	- [done] Having a template inside a loop does not seem to work correctly. The dynamically bound fields in the template does not display/render anything (looks like same issue with any bound data).
	- [done] Container and sections and such needs border options so we can have borders.

Templates:
	- [done] We need to preship a post template that we can use in the default blog index loop. This way we ensure it works to bind dynamical data in the template. Should be like this:
	 .--------.
	 |FEAT_IMG|
	 |Title   |
	 |Excerpt |
	 |Meta    |
	 '--------'
	Rounded edges, box shadow, featured image spanning 100% container width (centered). Max height for image: decide. Use inter for the fonts (make sure same font is used in every template).
	- [done] We need to ensure there is always a default template all the time. Suggestion: make sure the prebuilt templates cannot be deleted, but they can be edited, other suggestions on this matter?

Frontend:
	- [done] Hide the topbar when the user is not signed in. We want to hide the login page url.

Installer/general:
	- [done] We should ask the user what the user tends to use the blog for, like what genre. Needs a list of pre-defined themes the user can choose for, or none at all.
	- [done] Based on the theme, we should add 10 factoid posts about that theme as a default seeding.
